{
  "name": "smart-devices",
  "version": "1.0.0",
  "private": true,
  "description": "Smart Devices PWA packaged with Capacitor for Android.",
  "scripts": {
    "build": "node scripts/build-web.mjs",
    "build:android": "npm run build && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor-community/bluetooth-le": "^8.0.0",
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
